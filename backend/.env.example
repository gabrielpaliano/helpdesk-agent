OPENAI_API_KEY=sk-...
PORT=3000

# Optional: change model (gpt-4o-mini is cheaper for dev, gpt-4o for prod)
OPENAI_MODEL=gpt-4o-mini

# Rate limiting: max requests per session per minute
RATE_LIMIT_MAX=10
RATE_LIMIT_WINDOW_MS=60000
